[tool.poetry]
name = "lecture-intelligence-system"
version = "0.1.0"
description = "Lecture Intelligence System scaffold"
authors = ["Ibrahim Kako"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
